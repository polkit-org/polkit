policy = 'org.freedesktop.policykit.policy'

i18n.merge_file(
  input: policy + '.in',
  output: '@BASENAME@',
  po_dir: po_dir,
  data_dirs: its_dir,
  install: true,
  install_dir: pk_pkgactiondir,
)
